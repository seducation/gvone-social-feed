import React from "react";
import { ArrowLeft, ArrowUpRight, Check, ExternalLink, FolderPlus, Layers3, Loader2, Power, PowerOff, Trash2, X } from "lucide-react";

type ManagedFeed = { id: number; title: string; customTitle: string | null; url: string; description: string | null; faviconUrl: string | null; lastFetchedAt: Date | string | null; isEnabled: boolean };
type ManagedGroup = { id: number; name: string };
type ManagedArticle = { id: number; title: string; link: string; description: string | null; publishedAt: Date | string | null };

const fallbackIcon = "https://www.google.com/s2/favicons?domain=rss.app&sz=64";
const sourceName = (feed: ManagedFeed) => feed.customTitle || feed.title;
const formatDate = (value: Date | string | null) => value ? new Date(value).toLocaleDateString(undefined, { month: "short", day: "numeric", year: "numeric" }) : "Not refreshed yet";

export function SourceManager({ feeds, groups, selectedFeedId, articles, isLoadingArticles, onClose, onOpenFeed, onOpenGroup, onCreateGroup, onBack, onSetEnabled, onRemove, pendingFeedId }: {
  feeds: ManagedFeed[];
  groups: ManagedGroup[];
  selectedFeedId: number | null;
  articles: ManagedArticle[];
  isLoadingArticles: boolean;
  onClose: () => void;
  onOpenFeed: (feedId: number) => void;
  onOpenGroup: (groupId: number) => void;
  onCreateGroup: () => void;
  onBack: () => void;
  onSetEnabled: (feed: ManagedFeed) => void;
  onRemove: (feed: ManagedFeed) => void;
  pendingFeedId: number | null;
}) {
  const selectedFeed = feeds.find((feed) => feed.id === selectedFeedId);
  const enabledCount = feeds.filter((feed) => feed.isEnabled).length;

  const sourceList = <div className="min-h-0 flex-1 overflow-y-auto px-5 py-6 sm:px-7"><div className="flex flex-wrap items-center justify-between gap-3"><div><div className="text-xs font-semibold uppercase tracking-[.16em] text-[#8e96a3]">Collections</div><h3 className="mt-1 text-lg font-semibold">Category group feeds</h3></div><button type="button" onClick={onCreateGroup} className="inline-flex items-center gap-2 rounded-full bg-[#14161a] px-3.5 py-2 text-xs font-semibold text-white transition hover:bg-[#2b2e34]"><FolderPlus className="h-3.5 w-3.5" /> Create group</button></div><p className="mt-2 text-sm text-[#7f8794]">Open a category to read the combined articles from its selected RSS sources.</p><div className="mt-4 grid gap-3 sm:grid-cols-2">{groups.length ? groups.map((group) => <button key={group.id} type="button" onClick={() => onOpenGroup(group.id)} aria-label={`Open ${group.name} group feed`} className="group rounded-2xl border border-[#e4e7ed] bg-white p-4 text-left transition hover:-translate-y-0.5 hover:border-[#beb9ff] hover:shadow-[0_10px_24px_rgba(31,34,48,.06)]"><div className="flex items-center gap-3"><span className="grid h-9 w-9 place-items-center rounded-xl bg-[#eeedff] text-[#635bff]"><Layers3 className="h-4 w-4" /></span><span className="min-w-0 flex-1"><span className="block truncate text-sm font-semibold">{group.name}</span><span className="mt-1 block text-xs text-[#8b93a0]">Combined private feed</span></span><ArrowUpRight className="h-4 w-4 text-[#a8afba] group-hover:text-[#635bff]" /></div></button>) : <div className="rounded-2xl border border-dashed border-[#dce0e7] bg-white/60 p-5 text-sm text-[#8a929f]">No category groups yet. Create one by selecting your RSS sources.</div>}</div><div className="mt-8 flex items-center justify-between"><div><div className="text-xs font-semibold uppercase tracking-[.16em] text-[#8e96a3]">Sources</div><h3 className="mt-1 text-lg font-semibold">Manage RSS sources</h3></div><span className="rounded-full bg-[#eeedff] px-3 py-1.5 text-xs font-semibold text-[#635bff]">{enabledCount} active / {feeds.length}</span></div><p className="mt-2 text-sm text-[#7f8794]">Open a source to read its full saved history, or toggle and remove it here.</p><div className="mt-4 space-y-3">{feeds.length ? feeds.map((feed) => <article key={feed.id} className={`rounded-2xl border bg-white p-4 transition ${feed.isEnabled ? "border-[#e5e8ee] hover:border-[#cbc8ff]" : "border-[#e9ebef] opacity-80"}`}><div className="flex flex-col gap-4 sm:flex-row sm:items-center"><button type="button" onClick={() => onOpenFeed(feed.id)} aria-label={`Open ${sourceName(feed)} feed`} className="flex min-w-0 flex-1 items-center gap-3 text-left"><img src={feed.faviconUrl || fallbackIcon} alt="" className="h-10 w-10 rounded-xl bg-[#f4f5f8]" /><span className="min-w-0"><span className="flex items-center gap-2"><span className="truncate text-sm font-semibold">{sourceName(feed)}</span><span className={`rounded-full px-2 py-0.5 text-[10px] font-semibold ${feed.isEnabled ? "bg-[#e9faf5] text-[#168a6a]" : "bg-[#f0f1f4] text-[#727b89]"}`}>{feed.isEnabled ? "Enabled" : "Disabled"}</span></span><span className="mt-1 block truncate text-xs text-[#8a929f]">{feed.url}</span><span className="mt-1 block text-[11px] text-[#a2a9b4]">Updated {formatDate(feed.lastFetchedAt)}</span></span></button><div className="flex shrink-0 items-center gap-2"><button type="button" onClick={() => onOpenFeed(feed.id)} className="rounded-full border border-[#e1e4ea] px-3 py-2 text-xs font-semibold text-[#68707d] transition hover:border-[#635bff] hover:text-[#635bff]">Open</button><button type="button" onClick={() => onSetEnabled(feed)} disabled={pendingFeedId === feed.id} aria-label={`${feed.isEnabled ? "Disable" : "Enable"} ${sourceName(feed)}`} className={`grid h-9 w-9 place-items-center rounded-full transition disabled:opacity-50 ${feed.isEnabled ? "bg-[#fff1f1] text-[#d65d5d]" : "bg-[#e9faf5] text-[#168a6a]"}`}>{pendingFeedId === feed.id ? <Loader2 className="h-4 w-4 animate-spin" /> : feed.isEnabled ? <PowerOff className="h-4 w-4" /> : <Power className="h-4 w-4" />}</button><button type="button" onClick={() => onRemove(feed)} disabled={pendingFeedId === feed.id} aria-label={`Remove ${sourceName(feed)}`} className="grid h-9 w-9 place-items-center rounded-full bg-[#f5f6f8] text-[#838c99] transition hover:bg-[#fff0f0] hover:text-[#d65d5d] disabled:opacity-50"><Trash2 className="h-4 w-4" /></button></div></div></article>) : <div className="rounded-2xl border border-dashed border-[#dfe3ea] bg-white/70 p-10 text-center text-sm text-[#8a929f]">Add a source from the reader to manage it here.</div>}</div></div>;

  const sourceDetail = selectedFeed ? <div className="min-h-0 flex-1 overflow-y-auto px-5 py-6 sm:px-7"><div className="rounded-2xl border border-[#e5e8ee] bg-white p-5"><div className="flex flex-wrap items-start justify-between gap-4"><div className="min-w-0"><span className={`rounded-full px-2.5 py-1 text-xs font-semibold ${selectedFeed.isEnabled ? "bg-[#e9faf5] text-[#168a6a]" : "bg-[#f0f1f4] text-[#727b89]"}`}>{selectedFeed.isEnabled ? "Enabled" : "Disabled"}</span><p className="mt-3 break-all text-xs text-[#89919e]">{selectedFeed.url}</p>{selectedFeed.description && <p className="mt-3 max-w-2xl text-sm leading-6 text-[#68707d]">{selectedFeed.description}</p>}</div><a href={selectedFeed.url} target="_blank" rel="noreferrer" className="inline-flex items-center gap-2 rounded-full border border-[#e1e4ea] px-3 py-2 text-xs font-semibold text-[#68707d] transition hover:border-[#635bff] hover:text-[#635bff]">Visit source <ExternalLink className="h-3.5 w-3.5" /></a></div></div><div className="mt-6 flex items-center justify-between"><div><h3 className="text-base font-semibold">All saved stories</h3><p className="mt-1 text-sm text-[#8a929f]">Every stored article from this RSS source.</p></div><span className="rounded-full bg-[#eeedff] px-3 py-1.5 text-xs font-semibold text-[#635bff]">{articles.length} stories</span></div>{isLoadingArticles ? <div className="grid min-h-48 place-items-center"><Loader2 className="h-5 w-5 animate-spin text-[#635bff]" /></div> : articles.length ? <div className="mt-4 space-y-3">{articles.map((article) => <a key={article.id} href={article.link} target="_blank" rel="noreferrer" className="group block rounded-2xl border border-[#e5e8ee] bg-white p-4 transition hover:-translate-y-0.5 hover:border-[#cbc8ff] hover:shadow-[0_10px_24px_rgba(31,34,48,.06)]"><div className="flex gap-4"><span className="min-w-0 flex-1"><span className="line-clamp-2 block font-semibold leading-6 group-hover:text-[#635bff]">{article.title}</span>{article.description && <span className="mt-2 line-clamp-2 block text-sm leading-5 text-[#737c89]">{article.description}</span>}<span className="mt-3 block text-xs text-[#9aa1ad]">{formatDate(article.publishedAt)}</span></span><ArrowUpRight className="mt-1 h-4 w-4 shrink-0 text-[#a9b0bc] group-hover:text-[#635bff]" /></div></a>)}</div> : <div className="mt-4 rounded-2xl border border-dashed border-[#dfe3ea] bg-white/70 p-10 text-center text-sm text-[#8a929f]">This source does not have saved articles yet.</div>}</div> : sourceList;

  return <div role="dialog" aria-modal="true" aria-label="Manage RSS sources" className="fixed inset-0 z-50 bg-[#14161a]/45 p-3 backdrop-blur-sm sm:p-6"><section className="mx-auto flex h-full max-w-5xl flex-col overflow-hidden rounded-[1.75rem] bg-[#f7f8fa] shadow-[0_28px_90px_rgba(15,16,25,.35)]"><header className="flex items-center justify-between border-b border-[#e3e6ec] bg-white px-5 py-4 sm:px-7"><div>{selectedFeed ? <button type="button" onClick={onBack} className="inline-flex items-center gap-2 text-sm font-semibold text-[#68707d] transition hover:text-[#635bff]"><ArrowLeft className="h-4 w-4" /> All sources</button> : <><div className="text-xs font-semibold uppercase tracking-[.16em] text-[#8d95a1]">Your private library</div><h2 className="mt-1 text-xl font-semibold tracking-[-.03em]">Manage RSS sources</h2></>}{selectedFeed && <h2 className="mt-1 text-xl font-semibold tracking-[-.03em]">{sourceName(selectedFeed)}</h2>}</div><button type="button" onClick={onClose} aria-label="Close RSS source manager" className="grid h-10 w-10 place-items-center rounded-full border border-[#e2e5eb] bg-white text-[#68707d] transition hover:text-[#14161a]"><X className="h-4 w-4" /></button></header>{sourceDetail}</section></div>;
}
